apiVersion: 1

datasources:
  - name: Prometheus

providers:
  - name: garderobus-default
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
