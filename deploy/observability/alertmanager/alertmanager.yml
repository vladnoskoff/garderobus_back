global:
  resolve_timeout: 5m

route:
  receiver: default
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 3h
  group_by: ['alertname']

receivers:
  - name: default
    webhook_configs:
      - url: http://webhook.site/placeholder
        send_resolved: true
